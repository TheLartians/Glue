@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(LHC)
find_dependency(LarsVisitor)
find_dependency(LarsEvent)

foreach(dependency @GLUE_LUA_LIB@ @GLUE_DUKTAPE_LIB@)
  find_dependency(${dependency})
endforeach()

include("${CMAKE_CURRENT_LIST_DIR}/GlueTargets.cmake")
check_required_components("@PROJECT_NAME@")
